name: testing file
on: 
    workflow_dispatch: 

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v2
            - name: run a script
              run: echo "hello world github actions"