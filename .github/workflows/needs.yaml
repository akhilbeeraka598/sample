name: job dependencies workflow
on: 
    workflow_dispatch: 
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v2

            - name: run first job
              run: |
                echo "running job1"

                # Force failure for testing purposes
                # exit 1
    job2:
        runs-on: ubuntu-latest
        needs: job1
        steps:
          - name: run second job
            run: |
                echo "job1 is completed, running job2"

    job3:
        runs-on: ubuntu-latest
        needs: job2
        steps:
          - name: run third job
            run: |
                echo "job2 is completed, running job 3"